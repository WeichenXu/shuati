#include <iostream>
#include <string>

// Iterate len form 1 to Len/3
// Try all possible solution

using namespace std;

class Solution {
public:
    bool isAdditiveNumber(string num) {
        int len = num.length()/3;
        for (int i=1; i<=len; ++i){
        
        }
    }
private:
    string addOne(const string& str){}
};
